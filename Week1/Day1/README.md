# Week 1 - Day 1: SQL Fundamentals

## Overview

This folder contains my Day 1 SQL practice completed as part of the Databricks Training program. It includes SQL exercises from **Q1 to Q35**, covering SQL fundamentals such as data retrieval, filtering, string matching, date functions, aggregate functions, grouping, HAVING, and sorting.

## Files

| File | Description |
|------|-------------|
| `create_tables.sql` | SQL script to create Department, Employee, and Project tables |
| `insert_data.sql` | SQL script to insert sample data into the tables |
| `queries.sql` | SQL practice queries (Q1–Q35) |
| `screenshots/` | Output screenshots of executed queries (if available) |

## Topics Covered

### Problem 1: Basic Queries
- SELECT statements
- Selecting specific columns
- Filtering rows using `WHERE`

### Problem 2: String Matching Queries
- `LIKE` operator
- Wildcards (`%`, `_`)

### Problem 3: Date Queries
- `YEAR()`
- `MONTH()`
- `CURRENT_DATE`
- `INTERVAL`

### Problem 4: Aggregate Queries
- `SUM()`
- `AVG()`
- `MIN()`
- `COUNT()`

### Problem 5: Group By Queries
- `GROUP BY`
- `MAX()`
- `AVG()`
- `SUM()`
- `COUNT()`

### Problem 6: Having Queries
- `HAVING`
- Filtering grouped records

### Problem 7: Order By Queries
- `ORDER BY`
- Ascending and descending sorting
- Sorting by multiple columns

## Total Progress

- ✅ Problems Completed: **7**
- ✅ Queries Completed: **35**
- ✅ Tables Used: **Department, Employee, Project**

## Learning Outcomes

By completing these exercises, I learned how to:

- Retrieve data using `SELECT`
- Filter records using `WHERE`
- Perform pattern matching with `LIKE`
- Work with date functions
- Use aggregate functions
- Group records using `GROUP BY`
- Filter grouped results using `HAVING`
- Sort query results using `ORDER BY`
